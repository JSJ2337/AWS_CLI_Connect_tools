# 작업 기록 — 2026-07-19

## 16:17 - 환경 진단(doctor) 기능 추가 (WIP)

### 변경 파일

- `ec2menu/core/doctor.py` (신규)
- `ec2menu/main.py` (`--doctor` 플래그, 메인 메뉴 '환경 진단' 항목)

### 내용

- `brew doctor` 패턴의 환경 진단 모듈 신규 작성. 필수 전제조건 점검:
  Python 3.9+, aws CLI, session-manager-plugin, boto3, AWS 자격증명(sts).
- `run_doctor()` 는 각 점검을 `OK/WARN/FAIL` 로 출력하고 FAIL 개수를 exit code 로 반환.
- CLI `--doctor` 플래그(파싱 직후 실행 후 종료) + 메인 메뉴 항목 양쪽에서 접근 가능.
- `check_aws_credentials()` 의 sts 검증 로직은 `TODO(human)` 자리표시자 상태
  (현재 `WARN` 반환) — 실패 유형별 분류는 후속 구현 예정.

### 이유

- SSM 세션/포트포워딩·자격증명 등 런타임 의존성이 많아, 실행 전 환경 문제를
  한 번에 진단할 수단이 필요. 신규 사용자 온보딩·트러블슈팅 시간 단축 목적.

### 영향도

- 단일 CLI 도구 로컬 범위. 기존 메뉴 인덱스 핸들러를 1칸 시프트(도움말 뒤 삽입)했으나
  기존 동작 불변. pytest 32 passed, `python3 -m ec2menu --doctor` exit 0 검증.

### 검증

- `pytest`: 32 passed
- `python3 -m py_compile ec2menu/core/doctor.py ec2menu/main.py`: OK
- `python3 -m ec2menu --doctor`: exit 0 (필수 4개 점검 통과, 자격증명은 WARN 자리표시자)
