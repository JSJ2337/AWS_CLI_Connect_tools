# 작업 이력 - 2026-04-20

## 22:15 - 구 버전 파일 archive 폴더로 이관

### 변경 사항

- 신규 디렉토리: `archive/`
- 이동 파일:
  - `ec2menu_v5.1.9.py` → `archive/ec2menu_v5.1.9.py` (Windows/WSL 구 버전)
  - `ec2menu_v5.3.0_mac.py` → `archive/ec2menu_v5.3.0_mac.py` (macOS 구 버전)
  - `ec2menu_v5.4.0_mac.py` → `archive/ec2menu_v5.4.0_mac.py` (macOS 구 버전)
  - `ec2menu_v5.5.0_mac.py.bak` → `archive/ec2menu_v5.5.0_mac.py.bak` (분리 전 단일 파일 백업)
- 루트 유지: `ec2menu_v5.5.0_mac.py` (현재 씬 래퍼, 진입점)
- `git mv` 사용으로 추적 파일 이력 보존, `.bak` 파일은 미추적 상태 그대로 `mv`

### 이유

- 루트 디렉토리 정리: 구 버전 스냅샷들이 최상위에 노출되어 현재 진입점(`ec2menu_v5.5.0_mac.py`)과 혼동 유발
- 패키지 구조 분리 완료 후 구 단일 파일 버전들은 참고용으로만 필요
- `archive/` 네이밍으로 "보관/참고용" 목적을 명시적으로 표현

### 영향도

- 영향 없음: 현재 진입점(`ec2menu_v5.5.0_mac.py`)과 패키지(`ec2menu/`)는 루트에 그대로 유지
- 기존 alias(`python3 ec2menu_v5.5.0_mac.py`) 동작 변화 없음
- `python3 -m ec2menu` 패키지 실행도 영향 없음

### 문서 업데이트

- `CLAUDE.md`
  - Project Structure 섹션에 `archive/` 디렉토리 추가
  - `conftest.py`, `pyproject.toml` 명시
  - Version Convention 섹션에 archive 이관 규칙 추가

### 검증

- `git status`로 `renamed:` 표시 확인
- 루트 `.py` 파일 목록: `conftest.py`, `ec2menu_v5.5.0_mac.py` 만 남음
