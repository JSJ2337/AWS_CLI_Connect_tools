# 작업 이력 - 2026-01-09

## 14:00 - ec2menu v5.4.0 macOS 버전 개선

### 변경 사항

- 변경된 파일: `ec2menu_v5.4.0_mac.py` (신규 생성, v5.3.0 기반)
- 변경 라인 수: 3,784 → 4,193 (+409 라인)

### Phase 1: 보안 강화

#### 1-1. 커맨드 인젝션 방지

- `shlex` 모듈 import 추가
- S3 파일 전송 명령어에 `shlex.quote()` 적용 (lines 466-515)
- 경로에 셸 메타문자가 포함된 경우에도 안전하게 처리

```python
safe_s3_key = shlex.quote(s3_key)
safe_remote_path = shlex.quote(remote_path)
command = f"aws s3 cp s3://{bucket_name}/{safe_s3_key} {safe_remote_path}"
```

#### 1-2. macOS Keychain 연동

- `KeychainManager` 클래스 추가 (lines 301-383)
- DB 패스워드를 macOS 보안 저장소에 저장/조회
- `get_db_credentials()` 함수 개선: Keychain, 메모리, 새 입력 3단계 조회
- 저장 옵션: Keychain 영구 저장 / 메모리만 / 저장 안함

### Phase 2: 성능 최적화

#### 2-1. 리소스별 캐시 TTL 차등 적용

- `Config.CACHE_TTLS` 딕셔너리 추가 (lines 217-227)
- `PerformanceCache._get_ttl_for_key()` 메서드 추가
- EC2/SSM: 2분, RDS/ElastiCache: 5분, ECS/EKS: 10분, 리전: 1시간

```python
CACHE_TTLS = {
    'instances': 120,      # EC2: 2분
    'ssm': 120,            # SSM: 2분
    'rds': 300,            # RDS: 5분
    'elasticache': 300,    # ElastiCache: 5분
    'ecs': 600,            # ECS: 10분
    'eks': 600,            # EKS: 10분
    'regions': 3600,       # 리전: 1시간
}
```

#### 2-2. 페이지네이션 헬퍼

- `paginate_display()` 함수 추가 (lines 436-505)
- 대량 결과를 20개씩 페이지 단위 표시
- 페이지 네비게이션: n(다음), p(이전), 번호 직접 선택

### Phase 3: 코드 품질 개선

#### 3-1. 헬퍼 함수 추출

- `get_menu_choice()`: 메뉴 입력 검증 헬퍼 (lines 396-431)
- 특수 키 매핑, 숫자 범위 검증, 빈 입력 처리 통합

#### 3-2. 배치 에러 추적 수정

- 배치 작업 실행 중 예외 발생 시에도 결과에 기록 (lines 1221-1235)
- `BatchJobResult` 객체 생성하여 `results` 리스트에 추가
- 전체 작업 상태를 정확하게 추적 가능

### Phase 4: 사용성 개선 (UX)

#### 4-1. 명령줄 옵션 확장

- `-s, --service`: 서비스 직접 진입 (ec2/rds/cache/ecs/eks)
- `--no-cache`: 캐시 비활성화
- `-v, --version`: 버전 표시
- 사용 예시 포함된 epilog 추가

```bash
python3 ec2menu_v5.4.0_mac.py -s ec2 -r ap-northeast-2
python3 ec2menu_v5.4.0_mac.py --no-cache
```

#### 4-2. 도움말 시스템

- `MENU_HELP` 딕셔너리: main, ec2, ecs, eks 메뉴별 도움말
- `show_main_help()`, `show_menu_help()` 함수 추가
- 메인 메뉴에서 `?` 입력 시 도움말 표시

### 영향도

- 영향받는 컴포넌트: 전체 CLI 도구
- 주의사항:
  - macOS Keychain 사용 시 최초 저장 시 사용자 권한 요청 팝업 발생
  - `--no-cache` 옵션 사용 시 API 호출 증가

---

## 검증 완료

- [x] Python 구문 검사: `python3 -m py_compile ec2menu_v5.4.0_mac.py`
- [x] `--help` 옵션 동작 확인
- [x] `--version` 옵션 동작 확인

## 실제 테스트 필요 항목

- [ ] EC2 인스턴스 목록 조회
- [ ] SSM 터미널 접속
- [ ] 배치 명령 실행 (재시도 포함)
- [ ] S3 파일 전송
- [ ] ECS/EKS 메뉴 접속
- [ ] 멀티 리전 전환
- [ ] Keychain 패스워드 저장/조회/삭제
- [ ] 화살표 키 네비게이션 동작 확인

---

## 16:30 - 화살표 키 네비게이션 추가 (curses)

### 변경 내용 (curses)

- 변경된 파일: `ec2menu_v5.4.0_mac.py`
- 변경 라인 수: 4,193 → 4,510 (약 +320 라인)

### 세부 구현

#### 5-1. InteractiveMenu 클래스 추가

- Python 내장 `curses` 모듈 사용
- 화살표 키(↑↓) 또는 vim 스타일(j/k)로 항목 이동
- Enter로 선택, q 또는 ESC로 취소
- `/` 입력으로 실시간 검색 필터링
- Page Up/Down으로 빠른 이동
- curses 실패 시 기존 번호 입력 방식으로 자동 폴백

```python
class InteractiveMenu:
    """curses 기반 화살표 키 네비게이션 메뉴"""

    def run(self) -> int:
        """선택된 인덱스 반환, 취소 시 -1"""
        try:
            return curses.wrapper(self._handle_input)
        except Exception:
            return self._fallback_menu()
```

#### 5-2. 화살표 네비게이션 적용 메뉴 목록

- Main Menu: 번호 입력 → 화살표 선택
- Region 선택: 번호 입력 → 화살표 선택
- EC2 인스턴스 목록: 번호 입력 → 화살표 선택
- ECS 클러스터/서비스/태스크: 번호 입력 → 화살표 선택
- EKS 클러스터/Pod/Namespace: 번호 입력 → 화살표 선택
- RDS 인스턴스 목록: 번호 입력 → 화살표 선택
- ElastiCache 클러스터 목록: 번호 입력 → 화살표 선택
- Jump Host 선택: 번호 입력 → 화살표 선택
- 최근 연결 기록: 번호 입력 → 화살표 선택

#### 5-3. 키 바인딩

- `↑` / `k`: 위로 이동
- `↓` / `j`: 아래로 이동
- `Page Up`: 10개 위로
- `Page Down`: 10개 아래로
- `Home` / `g`: 맨 위로
- `End` / `G`: 맨 아래로
- `Enter`: 선택
- `q` / `ESC` / `b`: 취소/뒤로
- `/`: 검색 모드 진입

### 영향도 (curses)

- 영향받는 컴포넌트: 모든 메뉴 선택 인터페이스
- 주의사항:
  - 터미널이 curses를 지원하지 않는 경우 자동으로 기존 방식으로 폴백
  - iTerm2, Terminal.app 모두 정상 동작
  - 배치 작업 등 다중 선택이 필요한 경우 기존 번호 입력 방식 유지

---

## 18:00 - ec2menu v5.5.0 macOS 버전 - AWS 서비스 확장

### 변경 사항 (v5.5.0)

- 변경된 파일: `ec2menu_v5.5.0_mac.py` (신규 생성, v5.4.0 기반)
- 변경 라인 수: 4,510 → 6,018 (+1,508 라인)

### Phase 1: CloudWatch 통합 구현

#### 1-1. AWSManager CloudWatch 메서드 (lines 2453-2652)

- `list_cloudwatch_dashboards()`: 대시보드 목록 조회
- `list_cloudwatch_alarms()`: 알람 목록 (상태 필터: OK/ALARM/INSUFFICIENT_DATA)
- `get_alarm_history()`: 알람 히스토리 조회
- `list_log_groups()`: 로그 그룹 목록
- `get_log_streams()`: 로그 스트림 목록
- `filter_log_events()`: 로그 이벤트 필터링 (시간 범위, 패턴)

```python
def list_cloudwatch_alarms(self, region: str, state: str = None,
                           force_refresh: bool = False) -> List[Dict]:
    """알람 목록 (상태 필터: OK/ALARM/INSUFFICIENT_DATA)"""
```

#### 1-2. CloudWatch 메뉴 함수 (lines 4850-5201)

- `cloudwatch_menu()`: CloudWatch 메인 메뉴 (대시보드/알람/로그 선택)
- `cloudwatch_dashboards_menu()`: 대시보드 목록 및 브라우저 열기
- `cloudwatch_alarms_menu()`: 알람 상태별 필터링 및 히스토리 조회
- `cloudwatch_logs_menu()`: 로그 그룹 탐색
- `cloudwatch_log_streams_menu()`: 로그 스트림 및 이벤트 조회

### Phase 2: Lambda 관리 구현

#### 2-1. AWSManager Lambda 메서드 (lines 2654-2829)

- `list_lambda_functions()`: 함수 목록 (런타임, 메모리, 최근 수정일)
- `get_lambda_function_detail()`: 함수 상세 정보
- `invoke_lambda_function()`: 함수 테스트 실행 (JSON 페이로드)
- `get_lambda_function_logs()`: CloudWatch Logs 연동 로그 조회
- `list_lambda_versions()`: 버전 목록
- `list_lambda_aliases()`: 별칭 목록

```python
def invoke_lambda_function(self, region: str, function_name: str,
                           payload: dict = None) -> Dict:
    """Lambda 함수 테스트 실행"""
```

#### 2-2. Lambda 메뉴 함수 (lines 5204-5468)

- `lambda_menu()`: Lambda 함수 목록
- `lambda_function_menu()`: 함수 상세 메뉴 (정보/테스트/로그/버전)
- `lambda_test_invoke()`: JSON 페이로드 입력 및 실행 결과 표시
- `lambda_logs_view()`: 최근 실행 로그 조회
- `lambda_versions_aliases()`: 버전/별칭 관리

### Phase 3: S3 브라우저 구현

#### 3-1. AWSManager S3 메서드 (lines 2831-3028)

- `list_s3_buckets()`: 버킷 목록 (글로벌)
- `get_bucket_location()`: 버킷 리전 조회
- `list_s3_objects()`: 객체 목록 (prefix/delimiter로 폴더 구조)
- `get_s3_object_info()`: 객체 상세 정보
- `download_s3_object()`: 다운로드 (진행률 콜백)
- `upload_s3_object()`: 업로드 (진행률 콜백)
- `generate_presigned_url()`: Presigned URL 생성 (기본 1시간)
- `delete_s3_object()`: 객체 삭제

```python
def download_s3_object(self, bucket_name: str, key: str,
                       local_path: str, progress_callback=None) -> bool:
    """S3 객체 다운로드 (진행률 콜백 지원)"""
```

#### 3-2. S3 브라우저 메뉴 함수 (lines 5471-5703)

- `s3_browser_menu()`: 버킷 목록 및 리전 표시
- `s3_bucket_browser()`: 폴더 구조 탐색 (prefix 기반)
- `s3_file_actions()`: 파일 다운로드/업로드/Presigned URL/삭제
- `format_size()`: 파일 크기 포맷팅 (B, KB, MB, GB)

### Phase 4: 메뉴 통합

#### 4-1. 메인 메뉴 확장 (lines 5888-5901)

```python
menu_items = [
    "🖥️ EC2 인스턴스 연결 (배치 작업 지원)",
    "🗄️ RDS 데이터베이스 연결",
    "⚡ ElastiCache 클러스터 연결",
    "🐳 ECS 컨테이너 연결",
    "☸️ EKS 클러스터 관리",
    "🌐 CloudShell 브라우저에서 열기",
    "📊 CloudWatch 모니터링",       # v5.5.0 신규
    "λ  Lambda 함수 관리",          # v5.5.0 신규
    "📦 S3 버킷 브라우저",           # v5.5.0 신규
    "📚 최근 연결 기록",
    "❓ 도움말",
]
```

#### 4-2. argparse 확장 (line 5887)

```python
parser.add_argument('-s', '--service',
    choices=['ec2', 'rds', 'cache', 'ecs', 'eks', 'cloudwatch', 'lambda', 's3'],
    help='직접 진입할 서비스 (v5.5.0: cloudwatch, lambda, s3 추가)')
```

#### 4-3. Config.CACHE_TTLS 확장 (lines 230-247)

```python
CACHE_TTLS = {
    'instances': 120,           # EC2: 2분
    'ssm': 120,                 # SSM: 2분
    'rds': 300,                 # RDS: 5분
    'elasticache': 300,         # ElastiCache: 5분
    'ecs': 600,                 # ECS: 10분
    'eks': 600,                 # EKS: 10분
    'regions': 3600,            # 리전: 1시간
    # v5.5.0 신규 서비스
    'cloudwatch_dashboards': 600,  # 대시보드: 10분
    'cloudwatch_alarms': 120,      # 알람: 2분
    'cloudwatch_logs': 60,         # 로그: 1분
    'lambda': 300,                 # Lambda: 5분
    's3_buckets': 600,             # S3 버킷: 10분
    's3_objects': 60,              # S3 객체: 1분
    'default': 300,                # 기본: 5분
}
```

#### 4-4. 도움말 추가 (lines 5791-5846)

- `MENU_HELP['cloudwatch']`: CloudWatch 대시보드/알람/로그 사용법
- `MENU_HELP['lambda']`: Lambda 함수 관리 및 테스트 실행 사용법
- `MENU_HELP['s3']`: S3 브라우저 탐색 및 파일 작업 사용법

### 영향도 (v5.5.0)

- 영향받는 컴포넌트: 전체 CLI 도구
- 주의사항:
  - S3 버킷은 글로벌 서비스이므로 리전 선택과 무관하게 전체 버킷 표시
  - Lambda 테스트 실행 시 실제로 함수가 실행되므로 주의 필요
  - CloudWatch 로그 조회 시 대량 로그 그룹이 있으면 시간 소요

---

## 검증 완료 (v5.5.0)

- [x] Python 구문 검사: `python3 -m py_compile ec2menu_v5.5.0_mac.py`
- [x] `--help` 옵션 동작 확인
- [x] `--version` 옵션 동작 확인 (v5.5.0 표시)

## 실제 테스트 필요 항목 (v5.5.0)

- [ ] CloudWatch 대시보드 목록 조회 및 브라우저 열기
- [ ] CloudWatch 알람 상태 필터링 (OK/ALARM)
- [ ] CloudWatch 로그 그룹/스트림 탐색 및 이벤트 조회
- [ ] Lambda 함수 목록 조회
- [ ] Lambda 테스트 실행 (JSON 페이로드)
- [ ] Lambda 로그 조회
- [ ] S3 버킷 목록 조회
- [ ] S3 폴더 구조 탐색
- [ ] S3 파일 다운로드/업로드
- [ ] S3 Presigned URL 생성 및 클립보드 복사
- [ ] S3 파일 삭제

---

## 20:00 - Pylance 타입 에러 수정 및 다중 리전 지원 개선

### 변경 사항 (타입 힌트)

- 변경된 파일: `ec2menu_v5.5.0_mac.py`
- 변경 내용:
  - Pylance 타입 에러 12개 수정
  - `Optional` 타입 힌트 추가

### 수정된 함수 시그니처

```python
# CloudWatch
def list_cloudwatch_alarms(self, region: str, state: Optional[str] = None, ...)
def list_log_groups(self, region: str, prefix: Optional[str] = None, ...)
def filter_log_events(..., log_stream: Optional[str] = None,
                      filter_pattern: Optional[str] = None,
                      start_time: Optional[int] = None,
                      end_time: Optional[int] = None, ...)

# Lambda
def invoke_lambda_function(..., payload: Optional[Dict] = None)

# S3
def download_s3_object(..., progress_callback: Optional[Callable] = None)
def upload_s3_object(..., progress_callback: Optional[Callable] = None)
```

---

## 20:30 - EKS/ECS 다중 리전 통합뷰 지원

### 변경 사항 (다중 리전)

- `eks_menu()`: 다중 리전 모드 지원 추가
- `ecs_menu()`: 다중 리전 모드 지원 추가
- 각 클러스터에 `_region` 필드 추가하여 원본 리전 추적

### 코드 변경 (EKS)

```python
if region == 'multi-region':
    regions = manager.list_regions()
    all_clusters = []
    for r in regions:
        clusters_in_region = manager.list_eks_clusters(r)
        for c in clusters_in_region:
            c['_region'] = r  # 리전 정보 저장
        all_clusters.extend(clusters_in_region)
    clusters = all_clusters
```

---

## 21:00 - 리전 선택 화면에 RDS/ElastiCache 표시

### 변경 사항 (리전 선택)

- `_check_region_resources()` 함수 확장
- EC2, ECS, EKS 외에 RDS, ElastiCache도 검색
- 리전 선택 화면에 리소스 종류별 태그 표시

### 코드 변경

```python
def _check_region_resources(manager: AWSManager, region: str) -> Dict[str, bool]:
    """리전에 EC2/ECS/EKS/RDS/ElastiCache 리소스가 있는지 확인합니다. (v5.5.0 확장)"""
    result = {'ec2': False, 'ecs': False, 'eks': False, 'rds': False, 'cache': False}
    # ... 각 서비스별 확인 로직
```

### 리전 선택 화면 예시

```text
--- [ AWS Regions with Resources ] ---
  1) ap-northeast-2      [EC2, ECS, RDS]
  2) us-east-1           [EKS, Cache]
 99) 모든 리전 통합 뷰
```

---

## 22:00 - curses 메뉴 화면 깜빡임 개선

### 변경 사항 (curses 최적화)

- 변경된 파일: `ec2menu_v5.5.0_mac.py`
- 변경 내용:
  - `stdscr.clear()` → `stdscr.erase()` 변경 (버퍼만 지움)
  - 색상 초기화를 `run()` 메서드에서 한 번만 수행
  - `_colors_initialized` 플래그 추가하여 색상 사용 조건부 처리
  - 유니코드 화살표 문자를 ASCII로 변경 (호환성)

### 코드 변경 (InteractiveMenu)

```python
class InteractiveMenu:
    def __init__(self, ...):
        ...
        self._colors_initialized = False

    def _draw_menu(self, stdscr):
        stdscr.erase()  # clear() 대신 erase() 사용
        ...
        if self._colors_initialized:
            stdscr.attron(curses.color_pair(1))

    def run(self):
        if curses.has_colors():
            curses.start_color()
            ...
            self._colors_initialized = True
```

### 변경된 문자

- `▶` → `>`
- `↑` → `^`
- `↓` → `v`
- `─` → `-`

### 영향도 (curses 최적화)

- 영향받는 컴포넌트: InteractiveMenu 클래스
- 주의사항: 화면 깜빡임이 줄어들지만 완전히 사라지지는 않을 수 있음

---

## 22:30 - simple-term-menu로 메뉴 시스템 교체

### 변경 사항 (메뉴 라이브러리 교체)

- 변경된 파일: `ec2menu_v5.5.0_mac.py`
- 변경 내용:
  - curses 기반 InteractiveMenu → simple-term-menu 라이브러리로 교체
  - 화면 깜빡임 문제 완전 해결
  - `import curses` 제거
  - `from simple_term_menu import TerminalMenu` 추가

### 의존성 추가

```bash
pip install simple-term-menu
```

### 코드 변경 (simple-term-menu)

#### 기존 (curses 기반)

```python
import curses

class InteractiveMenu:
    def run(self) -> int:
        stdscr = curses.initscr()
        # curses 초기화/종료 시 화면 깜빡임 발생
        curses.endwin()
```

#### 변경 후 (simple-term-menu)

```python
from simple_term_menu import TerminalMenu

def interactive_select(items, title="", footer="", show_index=True) -> int:
    if TERM_MENU_SUPPORT:
        menu = TerminalMenu(
            display_items,
            title=title,
            menu_cursor="> ",
            menu_highlight_style=("bg_cyan", "fg_black", "bold"),
            search_key="/",
            quit_keys=("escape", "q"),
            clear_screen=False,  # 화면 깜빡임 방지
        )
        return menu.show()
    else:
        return _fallback_menu(items, title, show_index)
```

### simple-term-menu 장점

- 화면 깜빡임 없는 부드러운 전환
- 화살표 키 / vim 키 (j/k) 지원
- `/` 키로 검색 기능 내장
- macOS/Linux 네이티브 지원
- 라이브러리 미설치 시 자동 폴백 (번호 입력 방식)

### 영향도 (simple-term-menu)

- 영향받는 컴포넌트: 모든 메뉴 선택 인터페이스
- 주의사항:
  - `pip install simple-term-menu` 필요 (미설치 시 번호 입력 방식으로 폴백)
  - 기존 InteractiveMenu 클래스는 하위 호환성을 위해 래퍼로 유지
