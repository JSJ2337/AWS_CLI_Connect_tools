# 작업 이력 - 2026-04-17

## 14:00 - ec2menu_v5.5.0_mac.py 모듈 분리 (단일 파일 → Python 패키지)

### 변경 사항

- 변경된 파일: `ec2menu_v5.5.0_mac.py` → 씬 래퍼로 변환 (5932줄 → 15줄)
- 신규 생성: `ec2menu/` 패키지 (23개 모듈, 약 3500줄)
- 신규 생성: `tests/test_imports.py`, `tests/test_core.py`
- 이유: 5932줄 단일 파일이 유지보수가 어려워 기능별 모듈로 분리

### 패키지 구조

```text
ec2menu/
├── __init__.py
├── __main__.py             ← python -m ec2menu 진입점
├── aws/
│   ├── manager.py          ← AWSManager (43개 메서드)
│   ├── batch.py            ← BatchJobManager
│   └── transfer.py         ← FileTransferManager
├── core/
│   ├── config.py           ← Config, IS_MAC
│   ├── cache.py            ← PerformanceCache 싱글톤
│   ├── colors.py           ← Colors, colored_text
│   ├── keychain.py         ← KeychainManager
│   └── utils.py            ← 공통 유틸
├── ui/
│   ├── menu.py             ← interactive_select, InteractiveMenu
│   ├── history.py          ← 연결 히스토리
│   └── credentials.py      ← DB 자격증명 관리
├── terminal/
│   ├── session.py          ← SSM/RDP/iTerm2/ECS Exec
│   └── kubectl.py          ← kubectl 래퍼, CloudShell
├── menus/
│   ├── ec2.py              ← ec2_menu, 배치/파일전송
│   ├── eks.py              ← eks_menu
│   ├── ecs.py              ← ecs_menu
│   ├── rds.py              ← connect_to_rds
│   ├── elasticache.py      ← connect_to_cache
│   ├── cloudwatch.py       ← cloudwatch_menu
│   ├── lambda_menu.py      ← lambda_menu
│   └── s3.py               ← s3_browser_menu
└── main.py                 ← choose_profile/region, main()
```

### 의존성 흐름 (단방향, 순환 없음)

```text
main.py → menus/* → aws/manager → core/*
main.py → menus/* → ui/* → core/*
main.py → menus/* → terminal/* → core/*
```

### 검증 결과

- 전체 모듈 구문 검사: `python3 -m py_compile` 통과
- 23개 모듈 import 체인: 모두 성공
- core 로직 테스트 (cache TTL, normalize_file_path, calculate_local_port): 통과

### 영향도

- 실행 방법 변경 없음: `python3 ec2menu_v5.5.0_mac.py` 기존대로 동작
- 신규 실행 방법 추가: `python3 -m ec2menu`
- 원본 파일 백업: `ec2menu_v5.5.0_mac.py.bak` (필요 시 복원 가능)
- 주의: `rds.py`, `elasticache.py`에서 `choose_jump_host`를 `ec2menu.main`에서 지연 임포트
  (circular import 방지 목적)
